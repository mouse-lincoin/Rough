/** Origin tracked by Y.UndoManager for local user edits */
export const LOCAL_ORIGIN = 'rough-local';

/** Origin for live preview updates during drag (not undoable) */
export const PREVIEW_ORIGIN = 'rough-preview';
